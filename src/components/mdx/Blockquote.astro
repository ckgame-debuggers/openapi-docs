---
// 이 컴포넌트는 일반 <blockquote> 태그에 전달될 모든 속성을 받습니다.
// <slot />을 사용하여 <blockquote> 태그 내부의 내용을 렌더링합니다.
---

<blockquote
  class="my-6 border-l-4 border-orange-500 bg-orange-50 py-4 pl-6 text-gray-700 dark:bg-white/2 dark:text-gray-300"
>
  <slot />
</blockquote>
